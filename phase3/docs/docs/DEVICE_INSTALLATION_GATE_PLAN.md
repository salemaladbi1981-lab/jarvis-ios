# JARVIS — Device Installation & Apple Real-Runtime Gate Plan

## Purpose
One consolidated owner/device validation session (not scattered interruptions).

## Gate contents
A. Installation — iPhone / iPad / macOS.
B. Calendar/Reminders — permission + real calendar.today + reminders.upcoming
   → closes M3.4 Real Data Runtime.
C. Live Voice — mic permission + real realtime session + streaming + Arabic/EN
   + barge-in + reconnect → closes M3.1 Live Runtime.
D. UI — seven states driven by real events, approval, alert.
E. Deferred Apple QA — true landscape, Split View, Reduced Motion, VoiceOver,
   offline, real-device performance.

## Preconditions
- TestFlight/installation path ready (Workstream B).
- OPENAI_API_KEY installed as server env secret.
- App installed on device.

## Output
- Real read evidence (M3.4 closure).
- Real voice evidence (M3.1 closure).
- Apple QA observations.
