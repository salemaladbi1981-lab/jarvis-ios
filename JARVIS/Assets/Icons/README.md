# Icons
Custom SVG/vector assets for visual-critical icons (per ICON-MAP.md).
P2.2 uses visually-equivalent SF Symbols via JarvisIconResolver (see
P2_2_ICON_PARITY.md). Custom vectors are dropped here when the reference
requires a glyph that SF Symbols cannot match.
